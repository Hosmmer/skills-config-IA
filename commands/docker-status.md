---
description: Ver estado de los contenedores Docker del proyecto
---

Ejecuta `make status` desde el root del proyecto para mostrar el estado de todos los contenedores.
Usa `make -C <project_root> status`.
